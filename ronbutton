<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>RONBUTTON</title>
  <style>
    body {
      margin: 0;
      font-family: sans-serif;
      background: #111;
      color: #fff;
    }

    #ronbutton {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: #444;
      border: none;
      border-radius: 50%;
      width: 70px;
      height: 70px;
      font-size: 24px;
      color: #fff;
      cursor: pointer;
      box-shadow: 0 0 10px #aaa;
    }

    #dialogo {
      position: fixed;
      bottom: 110px;
      right: 20px;
      background: #222;
      padding: 15px;
      border-radius: 10px;
      width: 300px;
      max-height: 400px;
      overflow-y: auto;
      display: none;
      box-shadow: 0 0 15px #000;
    }

    #dialogo p {
      margin: 10px 0;
    }

    input[type="text"] {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      background: #111;
      border: 1px solid #555;
      color: #fff;
      border-radius: 5px;
    }

    .respuesta {
      background: #333;
      padding: 10px;
      border-radius: 5px;
      margin-top: 10px;
      font-size: 14px;
    }

    .versiculo {
      font-style: italic;
      color: #ccc;
      margin-top: 5px;
    }
  </style>
</head>
<body>

<button id="ronbutton">👁️</button>

<div id="dialogo">
  <p><strong>Hablame:</strong></p>
  <input type="text" id="inputPregunta" placeholder="¿Qué te pasa hoy?" />
  <div id="respuestaContainer"></div>
</div>

<script>
  const btn = document.getElementById('ronbutton');
  const dialogo = document.getElementById('dialogo');
  const input = document.getElementById('inputPregunta');
  const respuestaContainer = document.getElementById('respuestaContainer');

  btn.onclick = () => {
    dialogo.style.display = dialogo.style.display === 'none' ? 'block' : 'none';
    input.focus();
  };

  input.addEventListener('keypress', function (e) {
    if (e.key === 'Enter') {
      const pregunta = input.value.trim();
      if (pregunta !== '') {
        mostrarRespuesta(pregunta);
        input.value = '';
      }
    }
  });

  function mostrarRespuesta(pregunta) {
    const lower = pregunta.toLowerCase();
    let respuesta = '';
    let versiculo = '';

    if (lower.includes('no doy más') || lower.includes('agotado') || lower.includes('estoy cansado')) {
      respuesta = 'Lo sé. Te pesa todo encima y no te alcanza el alma. Pero no estás solo, ni derrotado. Solo necesitás aflojar un rato y que alguien te vea de verdad.';
      versiculo = '“Venid a mí todos los que estáis trabajados y cargados, y yo os haré descansar.” (Mateo 11:28)';
    } else if (lower.includes('no me alcanza la plata')) {
      respuesta = 'No es culpa tuya si el mundo está hecho para que corras y no llegues. No te tragues la culpa: esto es sistema, no fracaso personal.';
      versiculo = '“Mi Dios, pues, suplirá todo lo que os falta conforme a sus riquezas en gloria en Cristo Jesús.” (Filipenses 4:19)';
    } else if (lower.includes('mi familia') || lower.includes('mis hijos')) {
      respuesta = 'Tu amor por ellos no se mide en plata ni en resultados. Ellos te miran y ven fuerza, aunque vos te sientas roto.';
      versiculo = '“Instruye al niño en su camino, y aun cuando fuere viejo no se apartará de él.” (Proverbios 22:6)';
    } else {
      respuesta = 'No tengo todas las respuestas. Pero si llegaste hasta este botón, es porque algo adentro tuyo todavía quiere seguir.';
      versiculo = '“El Señor está cerca de los quebrantados de corazón; y salva a los contritos de espíritu.” (Salmo 34:18)';
    }

    const block = document.createElement('div');
    block.className = 'respuesta';
    block.innerHTML = `
      <div>${respuesta}</div>
      <div class="versiculo">${versiculo}</div>
    `;
    respuestaContainer.prepend(block);
  }
</script>

</body>
</html>
